#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        long long n;
        cin >> n;
        string a;
        cin >> a;
        sort(a.begin(), a.end());
        cout << a << endl;
    }
}