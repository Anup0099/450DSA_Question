#include <bits/stdc++.h> 
#define ll long long int
using namespace std;
// Problem
// Chef has two nephews who love integers XX and YY. An integer NN is awesome if the following conditions hold:

// 1 \leq N \leq 10^{18}1≤N≤10 
// 18
 
// (N+Y)(N+Y) is divisible by XX.
// (N+X)(N+X) is divisible by YY.
// Find any awesome integer NN. We can prove that under the given constraints, an answer always exists.

// Note that you do not have to minimize the answer.

// Input Format
// The first line of input will contain a single integer TT, denoting the number of test cases.
// Each test case consists of a single line of input.
// The first line of each test case contains two space-separated integers XX and YY — favourite numbers of Chef's nephews.
// Output Format
// For each test case, output any awesome integer NN. 
int main() {
    int t;
    cin>>t;
    while(t--){
        ll x,y;
        cin>>x>>y;
        
        
    }
    return 0;
} 