#include <bits/stdc++.h>
using namespace std;
vector<vector<string>> mostPopularCreator(vector<string> &creators, vector<string> &ids, vector<int> &views)
{
}

int main() {
    int arr[1000]={0};
    array<int,5>arr;
    arr.fill(0);
    //iterators for rbegin() and rend();
    
}