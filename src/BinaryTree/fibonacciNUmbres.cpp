#include <bits/stdc++.h>
using namespace std;
// he defines a geeky numbers here the numbers is the sum of the k preceding numbers.
// you are given an array of size k,geeksNum[] where the ith element of the array represents
// the ith geeky number.Return the nth term.
long long solve(int N, int K, vector<long long> GeekNum)
{
    


}
int main() {}
